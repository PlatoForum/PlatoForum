<% content_for :title, @topic.name %>
<% content_for :og_title, "議題：" + @topic.name %>
<% content_for :og_description, "立場：" + @stance.description.nil? ? "立場#{@stance.number}" : @stance.description %>

<ul class="breadcrumb" style="margin-bottom: 5px;">
    <li><%= link_to "主頁", root_path %></li>
    <li><%= link_to "議題", "/list" %></li>
    <li><%= link_to @topic.name, "/#{@topic.permalink}/" %></li>
    <li><%= @stance.description.nil? ? "立場#{@stance.number}" : @stance.description %></li>
</ul>

<%= render 'comments/header' %>

<%= render 'stances/listview' %>