<div class="bs-component">
  <div class="modal fade" id="modal_support_comment">
    <div class="modal-dialog">
      <div class="modal-content">

        <% @support = Comment.new %>
        <% @support.owner = @proxy %>

        <%= form_for(@support, url: "/#{@topic.permalink}/comment_#{@comment.id}/reply", :html => { class: "form-horizontal"}) do |f| %>
        <fieldset>
          <input class="form-control" id="comment_stance" name="comment[stance]" placeholder="立場" type="hidden" value="support">

          <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
            <h4 class="text-success">支援評論</h4>
            <p class="text-muted">對於<span class="text-primary"><%= @comment.owner.display_name %></span>說：</p>
            <p class="text-muted"><%= @comment.body %></p>
            <p class="text-muted">你（<span class="text-primary"><%= @proxy.display_name %></span>）表示<span class="text-success">支持！</span></p>
          </div>

          <div class="modal-body">
            <div class="form-group">
              <label for="description" class="col-sm-2 control-label">內容</label>
              <div class="col-sm-10">
                <%= f.text_area :body, class: "form-control", placeholder: "評論內容", rows: "5" %>
                <span class="help-block">必要</span>
              </div>
            </div>

            <div class="form-group">
              <label for="name" class="col-sm-2 control-label">摘要</label>
              <div class="col-sm-10">
                <%= f.text_field :subject, class: "form-control", placeholder: "評論摘要" %>
                <span class="help-block">非必要，最多二十個字</span>
              </div>
            </div>
          </div>

          <div class="modal-footer">
            <div class="form-group">
              <div class="col-lg-10 col-lg-offset-2">

                <%= f.submit "送出", :class => 'btn btn-primary'%>
                <button href="#" data-dismiss="modal" class="btn btn-default">返回</button>
              </div>
            </div>
          </div>

        </fieldset>
        <% end %>

      </div>
    </div>
  </div>
</div>
<div class="bs-component">
  <div class="modal fade" id="modal_oppose_comment">
    <div class="modal-dialog">
      <div class="modal-content">

        <% @oppose = Comment.new %>
        <% @oppose.owner = @proxy %>

        <%= form_for(@oppose, url: "/#{@topic.permalink}/comment_#{@comment.id}/reply", :html => { class: "form-horizontal"}) do |f| %>
        <fieldset>
          <input class="form-control" id="comment_stance" name="comment[stance]" placeholder="立場" type="hidden" value="oppose">

          <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
            <h4 class="text-danger">反駁評論</h4>
            <p class="text-muted">對於<span class="text-primary"><%= @comment.owner.display_name %></span>說：</p>
            <p class="text-muted"><%= @comment.body %></p>
            <p class="text-muted">你（<span class="text-primary"><%= @proxy.display_name %></span>）表示<span class="text-danger">反對！</span></p>
          </div>

          <div class="modal-body">
            <div class="form-group">
              <label for="description" class="col-sm-2 control-label">內容</label>
              <div class="col-sm-10">
                <%= f.text_area :body, class: "form-control", placeholder: "評論內容", rows: "5" %>
                <span class="help-block">必要</span>
              </div>
            </div>

            <div class="form-group">
              <label for="name" class="col-sm-2 control-label">摘要</label>
              <div class="col-sm-10">
                <%= f.text_field :subject, class: "form-control", placeholder: "評論摘要" %>
                <span class="help-block">非必要，最多二十個字</span>
              </div>
            </div>

          </div>

          <div class="modal-footer">
            <div class="form-group">
              <div class="col-lg-10 col-lg-offset-2">

                <%= f.submit "送出", :class => 'btn btn-primary'%>
                <button href="#" data-dismiss="modal" class="btn btn-default">返回</button>
              </div>
            </div>
          </div>
        </fieldset>
        <% end %>

      </div>
    </div>
  </div>
</div>